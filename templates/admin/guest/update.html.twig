{% extends 'admin.html.twig' %}

{% block admin %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit" class="btn btn-primary">Modifier</button>
	{{ form_end(form) }}
	<a href="{{ path('admin_guest_block', {id}) }}" class="btn btn-primary">{{blocked ? 'Débloquer' : 'Bloquer'}}</a>
{% endblock %}
